# Mod compatibility triggers
# [1.12] These ones need to load AFTER the parent mod since the parent mod doesn't use REPLACE_OR_CREATE for these

# Community Outfit Mod
# TEMPORARY - remove once COM fixes their file
com_is_active = {
    always = no
}

# Better Politics Mod
bpm_ig_enabled = {
    always = yes # This needs to be yes
}
